@model List<string>

<h3>Please find your results below!</h3>
<br />
@if (Model.Count != 0)
{
    foreach(var item in Model)
    {
        <span>@item.ToString()</span>
    }
}
else
{
    <span>Sorry, No Sequence found!</span>
}

